#include <stdio.h>

int main() {
    int num, digit;

    printf("Enter an integer: ");
    scanf("%d", &num);

    while (num > 0) {
        digit = num % 10;
        printf("%d\n", digit);
        num /= 10;
    }

    return 0;
}

OUTPUT:-
Enter an integer:23
2
3
